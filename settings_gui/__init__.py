"""
Automatic/pre-made GUIs for the 'settings' package (object-settings).
Various things (like strings) can be optionally configured from settings_gui.config.

Currently includes: tkinter, ttk
"""

from . import config
